# Batch 44 — Admin Login Nginx Access

Scripts:

- `configure_rsa_cms_nginx_admin_login_access.sh`
- `check_rsa_cms_nginx_admin_login_access.sh`

This batch exposes admin static/login files and admin auth endpoints only. It does not expose admin data APIs yet.
